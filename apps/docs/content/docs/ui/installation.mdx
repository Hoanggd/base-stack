---
title: Installation
description: Get started with Base Stack using the CLI tool to create a new project
---

## Prerequisites

- [Node.js](https://nodejs.org/) (version 18 or higher)
- [pnpm](https://pnpm.io/) package manager

## Quick Start

Create a new Base Stack application using the CLI tool:

### npm
```bash
npm create base-stack@latest
```

### pnpm
```bash
pnpm create base-stack@latest
```

### Available Options

- `--stack <stack>`: Choose the stack (must be "next" or "vite")
- `--docs`: Include documentation setup

## Project Structure

After running the CLI, you'll get a project with:

- **Components**: Pre-built UI components from the Base Stack UI package
- **Configuration**: Pre-configured TypeScript, ESLint, and build tools
- **Documentation** (if included): Setup for component documentation

## Development

Navigate to your new project and start development:

```bash
cd my-app
pnpm install
pnpm dev
```

## Building

To build your project for production:

```bash
pnpm build
```